version: '3'
services:
    app:
        build:
            context: .
            dockerfile: dockerfile
        network_mode: bridge
        ports:
            - 80:80
            - 25:25
        depends_on: 
            - db
        links: 
            - db
        volumes:
            - ./src/:/var/www/vendor/blackoutzz/framework/src/
            - ./app/controllers/:/var/www/controllers/
            - ./app/ui/:/var/www/ui/
            - ./app/assets/:/var/www/assets/
            - ./app/logs/apache/:/var/log/apache2/
            - ./config/php/:/usr/local/etc/php/
            - ./app/logs/sessions/:/tmp/php/sessions/
        environment: 
            - DATABASE_HOST=db
            - DATABASE_NAME=app 
            - DATABASE_PORT=3306
            - DATABASE_USER=admin
            - DATABASE_PASSWORD=3XukYjbnBHaP38uuSjphR
            - ADMIN_USERNAME=admin
            - ADMIN_PASSWORD=admin
            - ADMIN_EMAIL=admin@localhost
            - DELAY=30
    db:
        image: mariadb
        network_mode: bridge
        ports:
            - 3306:3306
        environment:
            - MYSQL_RANDOM_ROOT_PASSWORD=1
            - MYSQL_USER=admin
            - MYSQL_PASSWORD=3XukYjbnBHaP38uuSjphR
            - MYSQL_DATABASE=app
